package com.sejigner.glee.model

import com.sejigner.glee.R

data class WorkModel(val userName: String, val likeCount: Int, val workPicture: Int, val userPicture: Int, val likeIcon: Int = R.drawable.ic_like)
